import { type FunctionComponent } from "react";
import "./app.scss";

export const SubmoduleApp: FunctionComponent = () => {
	return <footer className="monovitality-footer">Submodule App</footer>;
};
